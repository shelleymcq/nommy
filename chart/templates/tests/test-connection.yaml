apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nommy.fullname" . }}-test-connection"
  labels:
    {{- include "nommy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nommy.fullname" . }}:{{ .Values.client.service.port }}']
  restartPolicy: Never
